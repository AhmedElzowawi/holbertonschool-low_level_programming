#include "lists.h"
/**
 * pop_listint - check the code
 * @head: parameter
 * Return: Always 0.
 */
int pop_listint(lintint_t **head)
{
	listint_t *point = *head;

	point = point->next;
	return ((*head)->n)
}
