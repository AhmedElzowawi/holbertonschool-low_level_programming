#include <stdlib.h>

/**
  * main - prints if number is positive, 0 or negative
  * Return: 0
  */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; ++a)
	{
	putchar(a);
	}

	putchar('\n');
	return (0);
}
