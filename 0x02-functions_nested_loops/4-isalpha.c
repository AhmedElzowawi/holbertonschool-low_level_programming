#include "main.h"

/**
  * _isalpha - prints if number is positive, 0 or negative
  * @c: charecter
  * Return: 0
  */
int _isalpha(int c)
{
	if (_isalpha(c))
	{
	return (1);
	}
	else
	{
		return (0);
	}
}
