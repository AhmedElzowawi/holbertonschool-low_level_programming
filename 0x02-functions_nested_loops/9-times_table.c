#include "main.h"

/**
  * times_table - prints if number is positive, 0 or negative
  * Return: 0
  */
void times_table(void)
{
int a, b, multiplay;

for (b = 0; b < 10; b++)
{
	a = 0;
	multiplay = (a * b);
